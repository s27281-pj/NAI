# Przygotowanie Danych
- Exportowanie danych z ankiety w formacie .CSV (comma-separated values) do pliku dane.csv
- Konwersja danch .CSV do formatu .JSON ( => dane.json)

# Przygotowanie Środowiska
- ```pip install pandas numpy scikit-learn requests openpyxl```

